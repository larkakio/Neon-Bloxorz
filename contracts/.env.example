# Never commit .env — add to shell only or copy from this file.

# Base mainnet (public; use your own Alchemy/Infura URL in production if needed)
BASE_RPC_URL=https://mainnet.base.org

# Deployer — set ONLY in your terminal, never in chat or git:
# export PRIVATE_KEY=0x...
